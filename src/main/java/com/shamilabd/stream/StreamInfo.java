package com.shamilabd.stream;

/**
 * @author Abdullin Shamil Ildusovich
 * @Date 16.05.2023
 */
public class StreamInfo {
    public static void main(String[] args) {
        /*
        intermediate methods (lazy) - может быть много. Возвращает стрим. Вызываются только при вызове терминального метода.
        terminal method (eager) - только один.
        Intermediate methods:
        map
        filter
        sorted
        concat*
        distinct
        peek
        flatMap
        limit
        skip
        mapToInt
        sum
        min
        max
        average

        Terminal methods:
        forEach
        reduce
        count
        Collectors.groupingBy
        Collectors.partitioningBy
        min
        max
         */
    }
}
